---
title: use-q
description: A type-safe, schema-driven API client for TypeScript and React, built on TanStack Query v5.
template: splash
hero:
  tagline: A type-safe, schema-driven API client for TypeScript and React, built on TanStack Query v5.
  actions:
    - text: Quick start
      link: /getting-started/quick-start/
      icon: right-arrow
      variant: primary
    - text: View on GitHub
      link: https://github.com/rithviknishad/use-q
      icon: external
---

import { Card, CardGrid } from "@astrojs/starlight/components";

## Why use-q?

`use-q` is a thin, opinionated layer that turns an OpenAPI-style schema into fully typed, cache-aware React hooks — without giving up the power of TanStack Query.

<CardGrid stagger>
  <Card title="End-to-end type safety" icon="seti:typescript">
    Path params, search params, request bodies, response shapes, and error
    payloads are all inferred from a single `RouteDefinition` map. No `any`
    leaks into your components.
  </Card>
  <Card title="Framework-agnostic core" icon="puzzle">
    `@use-q/api-client` is a tiny zero-dependency fetcher that runs in Node,
    Bun, Deno, edge workers, or the browser. Use it from server actions,
    loaders, or scripts — no React required.
  </Card>
  <Card title="Schema-driven invalidation" icon="random">
    Tag routes with `tags`/`invalidatesTags` and the React layer takes care of
    the rest. Mutations automatically refresh the right queries through a
    shared `TagRegistry`.
  </Card>
  <Card title="Optimistic updates that compose" icon="rocket">
    Update multiple caches per mutation with snapshot/rollback semantics built
    in. Combine with `additionalInvalidatesTags` for arbitrary fan-out.
  </Card>
  <Card title="OpenAPI codegen" icon="seti:json">
    Generate a typed schema directly from an OpenAPI 3.x spec with
    `use-q-codegen`. Pagination is detected automatically.
  </Card>
  <Card title="Bring your own QueryClient" icon="setting">
    Reuse a single `QueryClient` across multiple `createApiClient` instances,
    or hook into persistence and devtools — `use-q` never gets in the way.
  </Card>
</CardGrid>

## A quick taste

```ts
import { createApiClient } from "@use-q/api-client-react";

const schema = {
  listPosts: {
    method: "GET",
    path: "/facilities/{facilityId}/posts",
    pathParams: {} as { facilityId: string },
    response: {} as { id: string; title: string }[],
    tags: [{ type: "post", facilityId: (p) => p.facilityId }],
  },
  createPost: {
    method: "POST",
    path: "/facilities/{facilityId}/posts",
    pathParams: {} as { facilityId: string },
    body: {} as { title: string },
    response: {} as { id: string; title: string },
    invalidatesTags: [{ type: "post", facilityId: (p) => p.facilityId }],
  },
} as const;

export const api = createApiClient<typeof schema>(schema, {
  baseUrl: "https://api.example.com",
});

export const { useQ, useM, useQClient } = api;
```

Then in a component:

```tsx
function PostList({ facilityId }: { facilityId: string }) {
  const { data, isLoading } = api.useQ("listPosts", { pathParams: { facilityId } });
  const createPost = api.useM("createPost", { pathParams: { facilityId } });

  if (isLoading) return <p>Loading…</p>;
  return (
    <>
      {data?.map((p) => <article key={p.id}>{p.title}</article>)}
      <button onClick={() => createPost.mutate({ body: { title: "New post" } })}>
        Add
      </button>
    </>
  );
}
```

That's it — list refetches automatically after the mutation thanks to schema-defined tags.

## Where to next?

<CardGrid>
  <Card title="Install" icon="download">
    [Install `@use-q/api-client` and the React layer](/getting-started/installation/)
  </Card>
  <Card title="5-minute quick start" icon="rocket">
    [Build a typed list+create flow](/getting-started/quick-start/)
  </Card>
  <Card title="Define a schema" icon="document">
    [Anatomy of a `RouteDefinition`](/getting-started/schema-definition/)
  </Card>
  <Card title="API reference" icon="open-book">
    [Browse every option and hook](/api-reference/route-definition/)
  </Card>
</CardGrid>
